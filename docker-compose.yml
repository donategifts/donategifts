version: '3.3'

services:
  db:
    image: mariadb
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 'theoneandonly'
      MYSQL_DATABASE: 'donategifts'
      MYSQL_USER: 'jack'
      MYSQL_PASSWORD: 'sparrow'
    ports:
      - '3306:3306'
    expose:
      - '3306'
    volumes:
      - ./scripts:/docker-entrypoint-initdb.d

volumes:
  donategifts:
