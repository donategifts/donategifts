extends ../../master

block title
    title Sign Up | Donate Gifts

block scripts
    script.
        $(document).ready(() => {
            const root = ReactRoot(document.querySelector('#app-root'));
            root.render(
                React.createElement(
                    Signup
                )
            );
        });